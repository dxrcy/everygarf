alternative source apis
    add to readme
    add arg docs
    benchmark each source

dont do initial ping for small image count
    default: if <10 images, dont ping
    override with --always-ping or something

overhaul error handling
    difference between cloudflare 429 vs other 429?
    document variants
    rename variants

use mpsc to send error to main?

(chores)
    document public items
    readme
        make better
    linting
        standardize `use` statement order

refactor and clean up modules
    too many items in `lib.rs`
    make `use` statements consistent

add tests
    unit test simple functions
    integration test

feature: file tree structure
    add cli option `--tree`

