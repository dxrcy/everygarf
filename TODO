alternative source apis
    add to readme
    add arg docs
    benchmark each source

add tests :(

add cli options
    use tree file structure
    choose file format (jpg)

overhaul error handling
    use `thiserror`
    use enum, not string
        move error code into enum
    difference between cloudflare 429 vs other 429?

document public items

readme
    make better

linting
    standardize `use` statement order

